В целом я не могу назвать это задание тяжёлым, но была пара вещей, вызвавших у меня трудности.

1. Не сразу поняла, как написать директорию файла, из которого считываются цифры.
2. Сначала использовала функцию Split("") для разделения строки символов на массив, но это не сработало
и я вспомнила про великолепную функцию ToCharArray().
3. Поначалу не учла, что функция Update вызывается настолько часто, что когда делаешь одно нажатие на 
клавишу, программа может зарегистрировать 2-3 нажатия, из-за чего я ввела перерыв между нажатиями в 0,2 секунды.
4. Не хватало типа данных Pair<T, U>, как в C++, поэтому пришлось написать реализацию класса Pair.
5. Забыла, что char при переводе в int выдаёт не ту же самую цифру, а со смещением, поэтому отняла '0' у каждого символа.
